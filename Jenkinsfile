pipeline{
    agent any
    stages{
        stage('Check Node'){
            steps{
                echo 'Checking Node JS'
                node --version
                npm --version
            }
        }
    }
}