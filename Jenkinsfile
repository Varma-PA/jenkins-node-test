pipeline{
    agent any
    stages{
        stage('Check Node'){
            steps{
                echo 'Checking Node JS'
                sh 'node --version'
                sh 'npm --version'
            }
        }
    }
}